class interpreter {
  constructor (py) {
    this.python = py
  }

  interpretation () {
    const code = py
    let interpreterData = {
      line: 0,
      symbol: 0
    }
  }
}

export default interpreter
